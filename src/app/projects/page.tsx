import { FolderPlus } from "lucide-react";
import { ProjectCard } from "@/features/projects/project-card";
import { ProjectForm } from "@/features/projects/project-form";
import { ProjectService } from "@/services/project.service";
export const dynamic = "force-dynamic";
export default async function ProjectsPage() { const projects = await ProjectService.getProjectSummaries(); return <section className="space-y-8"><div className="flex flex-wrap items-end justify-between gap-4"><div><p className="mb-2 text-xs font-medium uppercase tracking-[0.16em] text-primary">Workspace</p><h1 className="text-3xl font-semibold tracking-tight sm:text-4xl">Projects</h1><p className="mt-2 text-sm text-muted-foreground">A clear home for every outcome you’re working toward.</p></div><ProjectForm /></div>{projects.length ? <div className="grid gap-4 sm:grid-cols-2 lg:grid-cols-3">{projects.map((project) => <ProjectCard key={project.id} project={project} />)}</div> : <div className="rounded-2xl border border-dashed bg-card px-6 py-20 text-center shadow-sm"><FolderPlus className="mx-auto mb-3 size-7 text-primary" /><h2 className="font-medium">Start with a project</h2><p className="mt-1 text-sm text-muted-foreground">A project gives the next few tasks a home.</p><div className="mt-5"><ProjectForm /></div></div>}</section>; }
