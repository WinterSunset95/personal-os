import { ArchiveRestore } from "lucide-react";
import { RestoreProjectButton, RestoreTaskButton } from "@/features/projects/archive-list";
import { ProjectService } from "@/services/project.service";
import { formatDate } from "@/lib/date";
export const dynamic = "force-dynamic";
export default async function ArchivePage() { const { archivedProjects, archivedTasks } = await ProjectService.getArchivedProjects(); const taskProjects = new Set<string>(archivedTasks.map((task: any) => task.projectId as string)); return <section className="space-y-7"><header><p className="mb-2 text-xs font-medium uppercase tracking-[0.16em] text-primary">Recoverable items</p><h1 className="text-3xl font-semibold tracking-tight">Archive</h1><p className="mt-2 text-sm text-muted-foreground">Nothing here is gone forever. Restore work whenever you need it.</p></header>{!archivedProjects.length && !archivedTasks.length ? <div className="rounded-2xl border border-dashed bg-card px-6 py-20 text-center shadow-sm"><ArchiveRestore className="mx-auto mb-3 size-7 text-primary" /><h2 className="font-medium">Nothing archived</h2><p className="mt-1 text-sm text-muted-foreground">Archived projects and tasks can be restored here.</p></div> : <div className="space-y-4">{archivedProjects.map((project: any) => <article key={project.id} className="flex flex-wrap items-center justify-between gap-4 rounded-2xl border bg-card p-5 shadow-sm"><div><h2 className="font-semibold">{project.name}</h2><p className="mt-1 text-sm text-muted-foreground">Archived {project.archivedAt ? formatDate(project.archivedAt.toISOString().slice(0, 10)) : ""}</p></div><RestoreProjectButton projectId={project.id} /></article>)}{[...taskProjects].filter((projectId: string) => !archivedProjects.some((project: any) => project.id === projectId)).map((projectId: string) => <section key={projectId} className="rounded-2xl border bg-card p-5 shadow-sm"><h2 className="font-semibold">Archived tasks</h2><div className="mt-3 divide-y">{archivedTasks.filter((task: any) => task.projectId === projectId).map((task: any) => <div key={task.id} className="flex items-center justify-between gap-3 py-3"><span className="text-sm font-medium">{task.title}</span><RestoreTaskButton taskId={task.id} projectId={projectId} /></div>)}</div></section>)}</div>}</section>; }


